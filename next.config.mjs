/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 13+ App Router is default; no experimental flags needed.
  // Edge-friendly by default; routes can override runtime as needed.
};

export default nextConfig;
