# Serverless Postgres connection string (Neon recommended)
# Format: postgres://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL=

# Optional token to protect the migration endpoint in production
MIGRATION_TOKEN=
